version: '3.8'
services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MOCK_MODE=true
    volumes:
      - ./:/app
  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    volumes:
      - ./frontend:/usr/share/nginx/html:ro
